type NewsGroup {
	_id: ID!
	parent: String
	name: String
	type: String
	count: String
	countChildFolder: Int
}

type Query {
	newsGroups: [NewsGroup!] @isAuthenticated
	newsGroup(_id: ID!): NewsGroup @isAuthenticated
}
