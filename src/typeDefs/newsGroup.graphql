enum Type {
	TYPENEWS
	LAST
	SPECIALTY
}

type NewsGroup {
	_id: ID!
	parent: String
	name: String
	type: Type!
	count: String
	countChildFolder: Int
}

type Query {
	newsGroups: [NewsGroup!]
	newsGroup(_id: ID!): NewsGroup
}
