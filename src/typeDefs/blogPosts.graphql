type BlogPosts {
	_id: ID!
	title: String
	sourceUrl: String
	newsType: String
	content: String
	state: Int
	newsGroups: NewsGroup
	createdAt: Float
}

type Query {
	blogPosts: [BlogPosts!]
	blogPost(_id: ID!): BlogPosts
}
